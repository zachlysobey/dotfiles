# Zach's dotfiles

## Some random notes

### Homebrew

I use homebrew to install some stuff.

#### a sample of stuff I install with brew:

-   `brew install nvm`
-   `brew install yarn`
-   `brew install tree`
